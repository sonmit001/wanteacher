<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="treeMapper">
	<select id="getCategoryList" resultType="hashMap">
		SELECT ID, PARENT_ID, TEXT, 
		            CASE WHEN HREF IS NULL OR HREF = '' THEN 'fa fa-folder'		                    
		                 ELSE 'fa fa-link'
		             END ICON ,
		             HREF
		  FROM CATEGORY
		 START WITH PARENT_ID = '#'
		 CONNECT BY PRIOR ID = PARENT_ID
	</select>	
	
	<update id="updateNodeText" parameterType="hashMap">
		UPDATE CATEGORY SET
		       TEXT = #{text}
		 WHERE ID = #{id}
	</update> 
</mapper>